#!/bin/python
# installer for elijahOS

import os,shutil

def makeDir(name):
  os.mkdir(name)
  return name
def makeFile(name, directory):
  os.system(f'touch {directory}/{name}')

makeFile("PERMISSIONS", makeDir("__eliprog_cache__"))
